# Dependency fragment for the host Django project.
# Do not install this as a standalone project; copy/merge into the main project's dependencies.

djangorestframework>=3.15
openai>=1.40
Pillow>=10.0
rapidfuzz>=3.9
pandas>=2.2
openpyxl>=3.1
